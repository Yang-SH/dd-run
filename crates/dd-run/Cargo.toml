[package]
name = "dd-run"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "dd-run 宿主 CLI（M0：扩展清单扫描与协议全链路自检）"
publish = false

[dependencies]
dd-host = { path = "../dd-host" }
serde_json = "1"
