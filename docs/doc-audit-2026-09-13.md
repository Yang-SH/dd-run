# 文档内容核对说明（2026-09-13）

> **状态**：生效中 ｜ **版本**：v1.0 ｜ **最后更新**：2026-09-13
> **关联**：[INDEX.md](./INDEX.md)（文档索引与格式规约）· [implementation.md](./implementation.md)（实施主线）

---

## 1. 核对范围与方法

### 1.1 范围

全部 24 份 Markdown 文档（`docs/` 下 21 份 + 仓库根 3 份），整理前合计 659.4 KB / 6,461 行。

### 1.2 事实源与判据

| 判据来源 | 说明 |
|---|---|
| **代码** | `crates/dd-protocol`（协议类型与错误码）、`crates/dd-host`（清单解析、进程管理）、`crates/dd-ext`（扩展运行时与内置扩展）、`crates/dd-gui`（宿主 UI） |
| **构建产物** | `dist/` 下文件的实际字节数（脚本实测） |
| **版本与历史** | 各 `Cargo.toml` 的 `version`、`git log` / `git tag` |
| **契约文档互证** | `protocol.md`（v1.0 冻结）与 `manifest-schema.md`（v1.0）互为基准 |

**判定原则**：代码为唯一事实源。文档与代码冲突即判 `❌ 错误`；代码有而文档未记判 `⚠️ 遗漏`；文档内部前后打架单列为 `矛盾`。

### 1.3 方法

先做全量结构扫描（行数、最长行、标题数、表格行、代码块、跨文件链接可达性），再对每份文档逐条提取**可验证的技术断言**（方法名、常量值、函数名、依赖版本、字段名、路径、编号、字节数、commit）与代码比对；同时对文档之间的重复叙述做交叉校验。

---

## 2. 结论摘要

| 文档 | ❌ 错误 | ⚠️ 遗漏/不完整 | 矛盾 | 处理 |
|---|---|---|---|---|
| `protocol.md` | 5 | 3 | 2 | 已修正，含 1 项待人工决策 |
| `manifest-schema.md` | 2 | 0 | 0 | 已修正 |
| `extensions.md` | 2 | 0 | 1 | 已修正 |
| `implementation.md` | 7 | 2 | 2 | 已修正 |
| `cmdpal-platform-agnostic-design.md` | 1 | 1 | 0 | 已修正 |
| `search-file.md` | 3 | 1 | 3 | 已修正 |
| `search-file-update.md` | 1 | 1 | 1 | 已修正 |
| `search-file-plan-review.md` | 2 | 1 | 0 | 已修正 |
| `search.md` | 2 | 1 | 1 | 已修正 |
| `refactor-layering-plan.md` | 2 | 1 | 0 | 已修正 |
| `apps-filtering-plan.md` | 0 | 1 | 0 | 已修正 |
| `memory-optimization-plan.md` | 2 | 0 | 0 | 已修正 |
| `icons-typography-plan.md` | 1 | 0 | 0 | 已修正 |
| `README.md` / `README.zh-CN.md` | 3 | 0 | 2 | 已修正 |
| `CHANGELOG.md` | 0 | 2 | 0 | 已补记，日期待补 |
| 8 份里程碑记录 | 0 | 0 | 0 | **内容未改动**（见 §5.3） |

**跨文档冲突 6 项**，见 §4。**待人工决策 6 项**，见 §6。

### 严重度最高的 5 项（已修正）

1. **`extensions.md` 教人写错代码**：Python 快速上手片段缺 UTF-8 stdout 设定，却在示例里发中文，照抄在中文 Windows 必乱码。
2. **`manifest-schema.md` §10 整段过时**：仍在描述「5 个内置 exe 编译期内嵌 → 物化 → spawn」，而实现早已是进程内直调。
3. **`implementation.md` ADR-1 自相矛盾**：正文称「每个扩展 = 独立子进程」，同文档 M9 章节又写内置扩展已改 in-process。
4. **`search-file.md` 依赖与函数名是错的**：要求引入并不存在的 `windows 0.62`，要求实现并不存在的 `pct_encode`。
5. **`protocol.md` 附录示例违反自己的规定**：§14 把 `invoke` 响应写成双层包裹，而 §6.5 明文禁止并说明会导致 `missing field 'kind'`。

---

## 3. 逐条核对清单

### 3.1 `docs/protocol.md`（扩展协议 v1.0，冻结契约）

| # | 位置 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| P-1 | §14 附录会话示例 | ❌ `invoke` 响应写作 `{"result":{"result":{...}}}` **双层包裹** | §6.5 明文规定「不要再包一层，多包一层会报 `missing field 'kind'`」——**文档违反自身规定** | ✅ 已改为单层 |
| P-2 | §9.2 错误码表 | ❌ 把「`get_command` 的 id 不存在」列为 `-32002` 触发 | §6.4 与 §9.2 注记均规定 `get_command` 查不到返回 `command: null` 属**正常结果**，非错误 | ✅ 已移除该触发场景 |
| P-3 | §9.2 错误码表 | ❌ `-32002` 全仓**只有常量定义、零抛出点**；`invoke` 未知 id 实际返回 `ShowToast{message:"未知"}` | `dd-protocol/src/messages.rs:31` 仅定义；`dd-ext/src/lib.rs:499-505` 返回 ShowToast | ⚠️ 已标注「保留码、待接线」；**是否实现待决策**（§6.1） |
| P-4 | §8.5 Page | ❌ 称页元信息「当前随 items 一并返回」 | `GetItemsResult` 仅含 `items`/`has_more_items`/`is_loading`（`messages.rs:132-136`）；`PageInfo` 无运行时分发路径，仅被一致性测试引用 | ✅ 已改为「类型已定义、运行时不传递」 |
| P-5 | §6.3 `has_more_items` | ❌ 说明提到「宿主可再次调用并带更大的 `offset`」，但参数表只有 `page_id`/`search_text` | 参数表自身无 `offset`；全仓无 `offset` 分页字段 | ✅ 已改为「v1.0 未定义分页参数」 |
| P-6 | §5.2 `initialized` | ⚠️ 称「收到 `initialized` 只用于触发一次额外的 `items_changed` 拉取」 | `process.rs:607` 仅把通知入队；`app/refresh.rs:38-45` 只消费 `items_changed` | ✅ 已改为「仅记录，不触发拉取」 |
| P-7 | §2.3 消息上限 | ⚠️ 称「扩展可回更低值」，但 `InitializeResult` 无承载字段 | `messages.rs:88-96` 无该成员 | ✅ 已标注未定义，需 MINOR 演进 |
| P-8 | §10 超时表 | ⚠️「进程启动 3000 ms」无对应常量 | 代码无该常量 | ✅ 已标注为文档建议值 |
| P-9 | 全文格式 | ⚠️ 2 处代码块缺语言标注；元信息块用尾随双空格硬换行 | — | ✅ 已统一 |

**核对通过（✅ 无需改动的核心断言）**：§1.3 方法总览（10 请求 + 2 通知）与代码常量一一对应；§8「8 种 Kind」属实（`model.rs:93-121`，CLI 的 `RESULT_KINDS` 亦为 8）；数据模型字段名与可选性全部吻合；错误码数值与 `-32001`/`-32003`/`-32005` 的实际抛出场景吻合；超时值（5000/3000/2000/2000/5000/10000/1000 ms）、1 MiB 上限、协议版本 `1.0`、宿主串行化、熔断 N=3、100 ms 合并窗口均与实现一致。

### 3.2 `docs/manifest-schema.md`（清单 schema v1.0，冻结契约）

| # | 位置 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| M-1 | §10 | ❌ 称「5 个扩展 exe 经 `build.rs` 编译期内嵌 → `embedded::materialize` 物化到 `cache/embedded/` → `ensure_builtins`」 | M9 B4 起 `build.rs` 的 `EMBED_EXES` 已清空为 `&[]`；`embedded.rs:3-6,56` 自述「恒为空物化路径，`materialize()` 返回 `None`」 | ✅ 已改写为 in-process 口径 + 模块现状说明 |
| M-2 | §10 | ❌ 称「进程隔离（ADR-1）不变：内置扩展仍是独立子进程」 | `ext_client.rs:48-51` 内置扩展走 `InProcess`，不 spawn | ✅ 已改为「隔离仅覆盖第三方/sidecar」 |

**核对通过**：§3 字段表 18 个字段的名称、必填性、默认值逐项命中 `manifest.rs:26-68`；§4 路径展开（`${EXT_DIR}`/`~`/相对/绝对）与 `expand_path` 一致；§7 九条校验与 `SkipReason` 一一对应；§8 与协议的关联表、§2 分发优先级（内置 > 用户目录 > sidecar）均与实现一致。

### 3.3 `docs/extensions.md`（扩展开发指南）

| # | 位置 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| E-1 | §5.4 | ❌「命令不存在时回 `-32002`（**不是** `ShowToast`）」——**与实现恰好相反** | `dd-ext/src/lib.rs:499-505` 对未知 id 返回 `CommandResult::ShowToast{message:"未知"}` | ✅ 已更正为事实，并加协议侧交叉引用 |
| E-2 | §1 第 3 步 | ❌ Python 快速上手片段**缺** `sys.stdout.reconfigure(encoding="utf-8", newline="\n")`，却又发送中文字面量（`"打个招呼"`/`"你好！"`） | 本文 §3 陷阱 2 明言该设定必做；中文 Windows 下 stdout 为 cp936，照抄必乱码 | ✅ 已在片段中补上设定并加注释 |

**核对通过**：§7 自检表 13 项与 `dd-run-cli --conformance` 的实际输出标签逐一对应；§1 第 4 步「Windows 自动补 `.exe`/`.cmd`/`.bat`」与 `resolve_executable`（`manifest.rs:280-296`）一致；§5.2「兜底绕开 `get_command`」、§8.3「Confirm 经 `confirmed=true` 重发」、§4「`initialized` 可选」均与实现一致。

### 3.4 `docs/implementation.md`（实施主线）

| # | 位置 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| I-1 | §4 ADR-1 | ❌ 称「每个扩展 = 独立子进程」，与同文档 M9 章节冲突 | `ext_client.rs` 内置走 in-process；`build.rs:20` `EMBED_EXES=&[]` | ✅ 改写为二分表述 |
| I-2 | 顶部状态块 | ❌「5 内置 exe 经 build.rs 内嵌 → 物化 → spawn（ADR-1 不变）」已被 M9 推翻 | 同上 | ✅ 已删改 |
| I-3 | 顶部状态块 | ❌ 状态只到 M5，缺 M6–M9；且仍称「剩余 C 组占位」（实则 L8 已代码完成） | 见本文件 §6.1 台账 L8 自述 | ✅ 已补 M6–M9 |
| I-4 | §2 里程碑表 | ⚠️ **漏记两个已落地批次**：v5.2 UI 优化 B1–B8、图标与字体优化 I1/I2/F1/F2 | `cmdpal-ui-optimization-v5.html` + git `815e212..da0589c`；`icons-typography-plan.md` + `3545d3f` | ✅ 已补入并标注 commit |
| I-5 | 全文产物描述 | ❌ `dist/dd-run-0.1.1.exe` 记为 8,553,984 B（实测 **8,601,088 B**）；多处称存在 `dist/dd-run.exe`（实际不存在）；`dist/dev/` 未记录 | 脚本实测 `dist/` 全部文件字节数 | ✅ 已按实测更正 |
| I-6 | M4 判据 A8 | ❌ 一处写「待真机复验」，后文写「真机复验通过」 | `m4-record.md` 为准 | ✅ 已统一为已通过 |
| I-7 | §6.1 遗留台账 | ❌ 一处已标 L5「✅ 已销项」，后文仍称「仅剩 L5/L9 开放」 | M6 批次 6.4 已销项 | ✅ 已统一 |
| I-8 | §7 附近 | ❌「已进入 M4」等过期表述 | 实际 M0–M9 全关闭 | ✅ 已移除 |
| I-9 | §7 标题 | ⚠️ 标题叫「下一步」，内容实为时间戳流水账 | — | ✅ 更名「实施沿革」 |
| I-10 | 全文格式 | ⚠️ 最长单行 **3,346 字符**（状态挤在一行）；体积 176.7 KB | 扫描实测 | ✅ 拆为状态表；34.6 KB / 最长行 303 字符 |

**核对通过**：版本号（5 crate = `0.1.1`、`dd-protocol` = `1.0.0`）与 `Cargo.toml` 一致；文档中出现的 **14 个 commit hash 全部经 `git cat-file` 验证为真实 commit**；tag `v0.1.0`/`v0.1.1` 存在；A1–A12 编号连续、无重复、无跳号，与设计文档及验收映射表一致。

### 3.5 `cmdpal-platform-agnostic-design.md`（平台无关设计）

| # | 位置 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| D-1 | §10 验收 | ⚠️ A1 / A5 / A8 三条**无量化阈值**（原文为定性描述） | 文档与该条上游下游均未给出数值 | ⚠️ 标注「未量化」，**未臆造数字** |
| D-2 | §10 验收 | ⚠️ A2 / A3 / A11 缺阈值 | `implementation.md` 有实测（A2 ~2 ms、A3 3.7 ms、A11 100% 键盘） | ✅ 已补实测值并注明来源里程碑 |
| D-3 | 全文 | ❌ 整理过程引入失效链接 `../docs/m9-inprocess-builtins.md`（该文档在仓库根，应写 `./docs/…`） | 链接可达性扫描 | ✅ 已修正 |

**核对通过**：A1–A12 定义与 `implementation.md` §3 验收映射、各 `mX-record.md` 的引用完全一致，**编号未被改动**（下游多处引用）。

### 3.6 `docs/search-file.md`（文件搜索权威方案）

| # | 位置 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| S-1 | §3.2 / T-08 / §7.2 | ❌ 要求实现 `pct_encode` 做 URL 编码——**该函数全仓零命中** | 实际函数为 `path_to_file_url`（`search.rs:99`），且**不做** percent-encode | ✅ 已改为实际函数并说明不做编码 |
| S-2 | §7.2 / §8.4.4 vs §7.4 | ❌ 超时**三处自相矛盾**：§7.2 与 §8.4.4 写「探测 800 ms / 搜索 3 s」，§7.4 写 1200 ms | 代码唯一常量 `ES_TIMEOUT = 1200ms`（`search.rs:53`） | ✅ 三处统一为 1200 ms |
| S-3 | §9.0 / §9.2 | ❌ 要求引入 `windows = "0.62"` 依赖 | `dd-ext/Cargo.toml` 只有 `everything-ipc` + `windows-sys 0.61`，**无 `windows`** | ✅ 已改为 `windows-sys 0.61` |
| S-4 | §5.3.4 / T-12 | ⚠️ `score_and_sort` 函数不存在 | 评分排序逻辑内联于 `search.rs:929-938` | ✅ 已标注 |
| S-5 | §1–§8 | ⚠️ **三层追注互相推翻**：v3.1 称 HTTP、v3.2 称被取代、v3.3 称 es.exe、§九 又称 everything-ipc | 代码实证：`everything-ipc` 主通道 + `es.exe` 回落 | ✅ 合并为单一权威口径 + 版本演进表 |

**核对通过**：`everything-ipc = "=0.1.4"`（`default-features = false`）、`fuzzy-matcher`、`chrono` 与 `Cargo.toml` 一致；`run_es()` / `everything_available()` / `decode_with_codepage()` / `EverythingClient::shared()` / `is_ipc_available()` / `is_db_loaded()` 均真实存在；清单三能力声明、免安装 sidecar 分发路径、二次输入 200 ms 去抖重拉、`f ` 前缀自动进页均与实现一致。

### 3.7 `docs/search-file-update.md` / `search-file-plan-review.md` / `search.md`

| # | 文档 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| U-1 | `search-file-update.md` | ❌ §2 表写「30% → 70%」，与 `search-file.md` §9.7 的 **25%** 冲突 | 两处口径来源不同 | ✅ 两口径并列并各自注明出处 |
| U-2 | `search-file-update.md` | ⚠️ §3.3 称 `query_wait`「无超时/待核实」 | 代码已加 `.timeout(1200)`（`search.rs:436,497`） | ✅ 已就地订正 |
| R-1 | `search-file-plan-review.md` | ❌ 写于 P2 落地**之前**，§五「编码前硬前置」已整体过时 | `search.rs:426-540` 已有 IPC 主通道 | ✅ 标注历史并逐条给最终处置 |
| R-2 | `search-file-plan-review.md` | ❌ W-09 把 `file://` 缺陷当现状 | 已修：`platform.rs` 改用 `resolve_file_url_to_path`，旧函数已删 | ✅ 已更正为已修复 |
| R-3 | `search-file-plan-review.md` | ⚠️ 全文行号漂移（`:284`→322、`:109`→128、`:666`→846 等） | 代码比对 | ✅ 行号改「约 :NNN」 |
| SH-1 | `search.md` | ❌ §六 称「引导项为通用文案」 | 已有专属 Toast（`search.rs:1093-1103`） | ✅ 已更正 |
| SH-2 | `search.md` | ❌ §六 称「二次输入只做本地过滤」 | 实为 200 ms 去抖后重拉（`app/page.rs:115`） | ✅ 已更正 |
| SH-3 | `search.md` | ⚠️ §七 称「IPC 直连尚未提供」 | 已实现 | ✅ 已更正 |

### 3.8 四个专题方案文档

| # | 文档 | 问题 | 依据 | 处理 |
|---|---|---|---|---|
| RL-1 | `refactor-layering-plan.md` | ❌ §2 目标结构 `ui/` 列 10 个文件，**实际 11 个**（缺 `ui/chrome.rs`） | 目录实测 | ✅ 已补 |
| RL-2 | `refactor-layering-plan.md` | ❌ §10.1 称 `main.rs` 103 行 | 实测约 150 行 | ✅ 已更正 |
| RL-3 | `refactor-layering-plan.md` | ⚠️ 全文行号漂移 | — | ✅ 改「约 :NNN」 |
| AF-1 | `apps-filtering-plan.md` | ⚠️ `app_list` 行号 284→约 293、`lnk_target` 736→约 733 | 代码比对 | ✅ 已更正 |
| MM-1 | `memory-optimization-plan.md` | ❌ M1 落点写作 `lifecycle.rs::hide()` | 实际在 `app/mod.rs` 约 :617-622（隐藏帧收尾） | ✅ 已更正 |
| MM-2 | `memory-optimization-plan.md` | ❌ API 名写作 `SetProcessWorkingSetSize` | 实为 `SetProcessWorkingSetSizeEx`（`platform.rs:16-21`） | ✅ 已更正 |
| IT-1 | `icons-typography-plan.md` | ❌ i18n key 写作 `settings.density.*` | 实际为 `set.density.*`（`text.rs:205-213`） | ✅ 已更正 |

**核对通过**：`apps-filtering-plan.md` 的黑/白名单、UWP `!` 豁免、参数化去重键、`_update` 后缀撤销四项均已在 `apps.rs` 落地；`memory-optimization-plan.md` 的 M2（`memmap2` + `Box::leak` + `from_static`）、M3（`icon_cache.clear()`）落点与描述一致；`icons-typography-plan.md` 的 F2 三档数值（36/13/11/20/16、40/14/12/24/20、44/15/13/28/24）与 `theme.rs:60-80` **逐值一致**。

### 3.9 `README.md` / `README.zh-CN.md` / `CHANGELOG.md`

| # | 问题 | 依据 | 处理 |
|---|---|---|---|
| RD-1 | ❌ 中英两版都把文件搜索算作「第 6 个内置扩展」 | 内置实为 5 个（apps/calc/system/websearch/shell）；文件搜索是独立 sidecar | ✅ 统一为「5 内置 + 文件搜索 sidecar」 |
| RD-2 | ❌ 中文版链接标注 `search-file.md`（v3.2），英文版标（v3.3） | 文档抬头确为 v3.3 | ✅ 中文版已改 v3.3 |
| RD-3 | ❌ 两版均写「embed + spawn / 进程隔离完整保留」 | M9 已改 in-process | ✅ 已改 |
| RD-4 | ⚠️ CHANGELOG `[Unreleased]` **漏记 3 项**：M9 内置进程内化（`4ff913f`/`2f0fe19`）、运行时内存优化（`1da6dbd`）、apps 垃圾项过滤（`0ab7d66`） | `git log v0.1.1..HEAD` 共 23 提交 | ✅ 已补记 |
| RD-5 | ⚠️ 部分条目**缺日期标注** | — | ⚠️ 保留原状，**未臆造日期** |

---

## 4. 跨文档冲突专项

同一事实在多份文档中被重复描述但**说法不一致**——这是本轮整理中"去冗余"的主要靶点。

| # | 事实 | 各处说法 | 代码真值 | 处置 |
|---|---|---|---|---|
| X-1 | 文件搜索超时 | `search-file.md` §7.2/§8.4.4 说 800 ms / 3 s；同文 §7.4 说 1200 ms；`search.md` 说 1.2 s | `ES_TIMEOUT = 1200 ms` | 三处统一为 1200 ms |
| X-2 | P2 IPC 落地状态 | `search-file.md` 后注称「已实施」；`search-file-plan-review.md` 称「编码前硬前置」；`search.md` 称「尚未提供」 | 已实现（`search.rs:426-540`） | 统一为已落地，历史稿标注归档 |
| X-3 | 页内二次输入行为 | `search-file.md` §7.4 说「已实时重拉」；`search.md` 说「只做本地过滤」 | 200 ms 去抖重拉 | 统一为 200 ms 去抖重拉 |
| X-4 | ECP 覆盖度 | `search-file-update.md` 说 70%（加权）；`search-file.md` §9.7 说 25%（条目） | 两者口径不同，非同一指标 | 并列保留、各自注明口径与出处 |
| X-5 | 空态引导项文案 | `search-file.md` 称「已解决」；`search.md` 称「仍为通用文案」 | 已有专属 Toast | 统一 |
| X-6 | 测试总数 | 各文档分别记 40/40、46/46、73/73、153/153、205、178、221、371 | 均为**不同时点的快照**，不是同一口径 | 各处补注基准时点，**不做统一**（强行统一反而失真） |

---

## 5. 整理动作汇总

### 5.1 结构层面

| 动作 | 说明 |
|---|---|
| 新建导航索引 | [`docs/INDEX.md`](./INDEX.md)：8 类分组（规范契约 / 设计与实施 / 开发指南与用户文档 / 专题方案 / 核查与评审报告 / 里程碑记录 / 归档 / 仓库入口）+ 6 条阅读路径 + 格式规约 + 未闭环项台账 |
| **未移动任何文件、未重命名任何文件** | 保持既有相对链接稳定；整理后**失效链接 0 个**（整理过程中新引入的 1 个已修正） |
| 统一元信息块 | 17 份文档采用统一头部：`状态 ｜ 版本 ｜ 最后更新` + 关联链接；状态取值收敛为 5 种（生效中 / 已冻结 / 已落地 / 历史归档 / 规划中） |
| 追注合并 | `search-file.md` 的三层互相推翻的追注合并为「传输层与依赖统一口径」+「版本演进表」；正文只保留生效口径 |
| 历史内容下沉 | `implementation.md` 的数十条时间戳流水账收敛为里程碑态表 + 文末「实施沿革」表，过程细节改由 `mX-record.md` 承载 |
| 结论先行 | 各文档开头给出结论/状态总表 |

### 5.2 规模变化

| 指标 | 整理前 | 整理后 | 变化 |
|---|---|---|---|
| 原有 24 份文档 · 总体积 | 659.4 KB | 503.0 KB | **−23.7%** |
| 原有 24 份文档 · 总行数 | 6,461 | 6,466 | +5（新增状态总表抵消了去冗余删减） |
| 新增交付物 | — | 2 份（[`INDEX.md`](./INDEX.md)、本核对说明） | +32.8 KB / +449 行 |
| 全部文档合计 | 24 份 / 659.4 KB | 26 份 / 535.8 KB | −18.7% |
| `implementation.md` 体积 | 176.7 KB | 34.6 KB | **−80.4%** |
| `implementation.md` 最长行 | 3,346 字符 | 303 字符 | **−90.9%** |
| `search-file.md` 行数 | 1,050 | 922 | −12.2% |
| 失效链接 | 0 | 0 | 持平 |

> 部分文档行数因**新增状态总表与统一元信息块**而略增，属"统一格式"的必要成本；去冗余的收益体现在体积与 `implementation.md` 的结构上。

### 5.3 刻意未改动的部分

| 内容 | 原因 |
|---|---|
| 8 份里程碑记录（`m0`–`m4`、`m2`/`m0` 验证报告、`m9`）内容 | 用户决定：验收证据类历史记录保持原样以维持可追溯性。仅在 `INDEX.md` 中登记并标注"内容保持原样" |
| `CHANGELOG.md` 无日期条目的日期 | 无法从 commit 之外的可靠来源取证，**不臆造** |
| 各文档测试数快照（40/40、73/73、178…） | 均为真实历史快照，只补注基准时点，不强行统一 |
| `cmdpal-platform-agnostic-design.md` 的 A1–A12 编号 | 下游（`implementation.md` §3、各里程碑记录）多处引用，改动会引发连锁失效 |

---

## 6. 待人工决策

### 6.1 需要你拍板的 6 项

| # | 事项 | 选项 | 影响面 |
|---|---|---|---|
| 1 | `-32002 command_not_found` 只定义不产出 | ✅ **已定稿（2026-09-17）**：判为**扩展侧可用的标准错误码**（Python 示例即产出；宿主与内置运行时不产出、回 `ShowToast` 属合法选择），`protocol.md` §9.2 注记已改写为终态口径 —— 不再是开放项 | 已关闭（协议 v1.0 零改动） |
| 2 | `PageInfo` 类型定义了但运行时不传递 | ✅ **已定稿（2026-09-17）**：**维持不传递**（v1.0 预留定义）；宿主页标题改由宿主侧信息提供（被点击项标题 / 本地化文案），`protocol.md` §8.5 注记已改写 | 已关闭（启用须走 §13 `MINOR`） |
| 3 | 里程碑记录是否也要统一格式 | ① 维持现状（本次做法）② 折成单份摘要 + 原文归档 | 可追溯性 vs 格式一致性 |
| 4 | 文件搜索真机验收 A-33-05…A-33-10 | ✅ 首轮已于 2026-09-15 执行 —— 见 [验收报告](./search-file-p2-acceptance-2026-09-15.md)（A-33-07/10 通过；A-33-06/08 部分；A-33-05 性能未达标） | 仍不得对外承诺「无需 `es.exe`」（A-33-06/08 未全通过） |
| 5 | `CHANGELOG.md` 缺日期的条目 | 需要你补充 | 仅影响可读性 |
| 6 | 测试总数缺少统一口径 | ✅ **已建立（2026-09-17）**：`implementation.md` **§3.1 测试基线台账** —— 口径统一为 `cargo test --workspace` 的 passed 总数，逐批追加（当前 **433**），任何下降视为回归；并记入两条使用注意（roundtrip 的 SKIP 陷阱、三关均为 debug profile） | 影响回归判据的可比性 |

### 6.2 建议的后续动作

1. **优先做 #4**：这是唯一阻塞用户承诺的项；通过后可把「无需 `es.exe`」写入用户文档。
2. **#1 与 #2 建议一并处理**：两者同为「协议里定义了但运行时未接线」的类型，宜统一决策口径（要么都接线，要么都标注为预留）。
3. **#6 建议建立基线台账**：当前各文档的测试数快照跨度从 40 到 371，回归时难以判断"是否变少"。

---

## 7. 核对方法与复现

本次核对可用以下方式复现：

```bash
# 1. 文档体检（行数 / 最长行 / 标题数 / 表格行 / 代码块 / 链接可达性）
#    结果写入 .docscan.txt 与 .doclinks.txt
python tools/docscan.py

# 2. 协议一致性（从 protocol.md 抽取全部 JSON 示例做反序列化断言）
cargo +stable-x86_64-pc-windows-gnu test -p dd-protocol
```

**协议一致性测试结果**：11 项全绿（`framing` 8 项 + `consistency` 3 项，含 `example_count_matches_mapping` 与 `every_example_deserializes_to_typed_contract`），确认本次对 `protocol.md` 的修正**未破坏任何被抽取的 JSON 示例**。
